<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wisata">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".features.event.revamp.DetailEventsActivity"></activity>
        <activity android:name=".features.galery.revamp.GalleriesActivity" />
        <activity android:name=".features.event.revamp.EventsActivity" />
        <activity android:name=".features.galery.revamp.JenisGalleryActivity" />
        <activity android:name=".features.event.revamp.JenisEventActivity" />
        <activity android:name=".features.galery.DetailGalleryActivity" />
        <activity
            android:name=".features.main.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".features.profil.VisiMisiActivity" />
        <activity android:name=".features.profil.TugasFungsiActivity" />
        <activity android:name=".features.profil.TentangActivity" />
        <activity android:name=".features.profil.StrukturOrganisasiActivity" />
        <activity android:name=".features.profil.SejarahActivity" />
        <activity android:name=".features.profil.ProkerActivity" />
        <activity android:name=".features.profil.TentangKabupatenIndragiriHilirActivity" />
        <activity android:name=".features.berita.BeritaDetailActivity" />
        <activity android:name=".features.event.EventDetailActivity" />
        <activity android:name=".features.kuliner.KulinerDetailActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".features.petawisata.PetaWisataActivity"
            android:label="@string/title_activity_peta_wisata" />
        <activity
            android:name=".features.destinasi.detaildestinasi.DetailDestinasiActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".features.kontak.KontakActivity" />
        <activity android:name=".features.jenisdestinasi.JenisDestinasiActivity" />
        <activity android:name=".features.galery.GaleryActivity" />
        <activity android:name=".features.kuliner.KulinerActivity" />
        <activity android:name=".features.profil.ProfilActivity" />
        <activity android:name=".features.event.EventActivity" />
        <activity android:name=".features.berita.BeritaActivity" />
        <activity android:name=".features.destinasi.DestinasiActivity" />
        <activity
            android:name=".features.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>